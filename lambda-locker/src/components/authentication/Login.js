import React from 'react'

import useLoginForm from '../../hooks.js'

const Login = () => {
    // alert(`User Logged In.  User email: ${inputs.email}`)
    // const { inputs, handleChange, handleSubmit } = useLoginForm()
    // return (
    //     <>
    //         <form onSumbit={handleSubmit}>
    //             <div>
    //                 <input type="text" name="email" required placeholder="email" onChange={handleChange} />
    //             </div>
    //             <div>
    //                 <input type="password" name="password" required placeholder="password" onChange={handleChange} />
    //             </div>
    //             <button type="submit">Login</button>
    //         </form>
    //     </>
    // )
    return null
}

export default Login